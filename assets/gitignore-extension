
### Project ###
# ignore local temporary folder for prototyping
# based on: https://stackoverflow.com/a/46354071/1423815
/.tmp/**
# trace every .gitkeep files to maintain directory structure integrity in the repository
# no kept folder can be declared after this line, otherwise the .gitkeep won't be tracked!
!**/.gitkeep

# ignore dotenv files to avoid leaking of sensitive configurations of the an application: https://12factor.net/config
.env
